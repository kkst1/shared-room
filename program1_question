下面是整理后的 60 个题目，每题附 1 到 2 个压力追问，目标不是考概念背诵，而是筛出是否真的做过这个项目。题目覆盖：K3 bring-up、U-Boot/Kernel 移植、Buildroot、OpenAMP/rpmsg、共享内存、ALSA、多线程 Pipeline、数据链路与排障。

一、项目总链路与架构

1. 你先把这个系统从上电到音频落盘的完整数据链路按时间顺序讲一遍，经过了哪些核、哪些驱动、哪些缓存区、哪些线程？
追问：每一跳的数据格式分别是什么，在哪里发生了格式转换？
追问：如果我让你画时序图，哪一段最容易丢帧，为什么？

2. 为什么这个项目要把采集放在 R5F，而不是直接让 A 核 Linux 去采 ADS8688？
追问：你实际测过 Linux 侧直接采集时的什么指标，证明它不合适？
追问：如果一定要放在 A 核，你会先担心中断延迟、SPI 抖动还是调度抖动，为什么？

3. 你的系统里“控制面”和“数据面”分别走什么链路？为什么不统一走 rpmsg？
追问：你们消息体里哪些字段属于控制面，哪些属于数据面元信息？
追问：如果强行全走 rpmsg，瓶颈会先出在带宽、中断频率还是拷贝次数？

4. Ping-Pong Buffer 的触发点在哪里，触发后是谁通知谁？
追问：触发时机是半满、全满还是 DMA 完成中断？为什么这样选？
追问：如果触发后通知丢了一次，系统会怎么恢复？

5. 共享内存里你放的是“音频原始数据”还是“描述符/索引”？这么设计的理由是什么？
追问：头部和 payload 的边界如何定义，靠固定偏移还是长度字段？
追问：如果以后要支持多路音频或不同采样格式，现有布局哪里最先要改？

6. 这个项目里吞吐、实时性、可维护性三者冲突时，你优先保哪一个？为什么？
追问：给一个你实际做过的取舍案例，不要抽象回答。
追问：这个取舍最后体现在哪个参数、哪个线程模型或哪段协议上？

二、K3 SoC 与 Bring-up

7. TI K3 的多级启动链路每一级职责是什么？你在哪一级投入最多排障时间，为什么？
追问：你在那个阶段最常看的日志或寄存器是什么？
追问：如果只能保留一个调试出口，你会保留串口、JTAG 还是某个 GPIO 心跳？

8. 修改版 BeagleBone AI 和原版板卡相比，哪些硬件差异会直接影响 U-Boot/Kernel 移植？
追问：这些差异最终落实到哪些文件，board 文件、设备树还是驱动？
追问：有没有哪处你一开始以为是软件问题，最后发现是板级改动导致？

9. 你第一次把板子点亮时，最先验证的三个信号或外设是什么？为什么不是别的？
追问：这三个验证项分别覆盖了哪三个层级的问题定位？
追问：如果三者里只有串口通，下一步你优先验证哪一项？

10. 如果串口没有任何输出，你会如何判断是 BootROM、SPL、U-Boot proper 还是 DDR 初始化前就挂了？
追问：不同阶段挂死时，供电、时钟、串口波形通常有什么差异？
追问：你有没有用过“打 GPIO 点”这类方法切分启动阶段？

11. DDR 初始化失败通常有哪些现象？你在项目里怎么定位过这类问题？
追问：你看过 DDR 训练日志、memory test 还是只靠启动现象判断？
追问：如果偶发失败而不是必现失败，你会先怀疑时序还是电源完整性？

12. K3 平台上电源域、时钟域、复位域的问题，通常会表现成什么症状？
追问：这三类问题在软件日志上的区别是什么？
追问：有没有遇到“设备树配了但外设还是像死的一样”的情况，最后怎么证实是 clock/reset？

13. 为什么 K3 上经常需要同时关心 A 核和 R5F 的 boot/clock/reset 状态，而不是只看 Linux 启没启动？
追问：如果 Linux 正常但 R5F 没跑起来，用户态通常会看到什么表象？
追问：你们项目里谁负责拉起 R5F，是 U-Boot、Linux remoteproc 还是别的机制？

14. 你做过哪些 pinmux 修改？如果 pinmux 配错，ADC 采集链路会出现什么现象？
追问：你怎么区分是 SPI pinmux 错还是 SPI 驱动参数错？
追问：有没有出现过“读出来有数据但全是错位/重复值”的情况？

15. 你如何验证某个外设是“硬件没通”还是“软件驱动没起来”？
追问：请按“最少侵入”的排查顺序回答，不要直接说示波器全量扫。
追问：如果是 SPI ADC，你最先抓哪几个信号？

三、U-Boot 移植

16. 你这个项目里 U-Boot 做了哪些板级修改？哪些是在 SPL，哪些是在 U-Boot proper？
追问：为什么这些修改不能都放在同一个阶段做？
追问：你改的内容里，哪个最容易导致“能进 SPL 但进不了 U-Boot proper”？

17. 为什么在 K3 平台上很多问题必须区分 SPL 阶段和 U-Boot proper 阶段？
追问：这两个阶段的可用驱动、内存条件、设备树使用方式有什么本质差异？
追问：你排障时怎么证明问题只发生在其中一个阶段？

18. 你有没有改过设备树在 U-Boot 阶段的内容？改它是为了解决什么问题？
追问：这些改动是否需要同步到 Linux 设备树，为什么？
追问：有没有出现 U-Boot DTS 和 Linux DTS 不一致导致的隐藏问题？

19. 如果 Linux 起不来，你在 U-Boot 阶段通常会先验证哪几件事？
追问：这几个验证项的顺序为什么这么排？
追问：你会优先验证镜像内容、加载地址、启动参数还是 dtb 兼容性？

20. 你如何确认 kernel、dtb、rootfs 这三者的版本和地址匹配是正确的？
追问：你有没有做过产物指纹或版本戳校验来避免混用？
追问：地址不匹配时，内核通常会挂在什么阶段？

21. 你有没有通过 U-Boot 单独验证过内存、串口、MMC、网络或某个自定义外设？怎么做的？
追问：你实际执行过哪些命令，不要只说“做过测试”。
追问：如果某个外设在 U-Boot 正常、Linux 异常，你会如何收窄范围？

四、Linux Kernel 移植与裁剪

22. 你在内核移植时改过哪些设备树节点？分别对应什么硬件功能？
追问：这些节点里哪些和 remoteproc/rpmsg 直接相关？
追问：你改过的节点里，哪个最容易因为地址或中断号写错而导致静默失败？

23. ADS8688 接入 Linux 这一侧时，你有没有在 A 核写内核驱动，还是完全绕开 Linux 驱动栈？为什么？
追问：如果没写内核驱动，那用户态如何保证访问时序和同步？
追问：如果以后要进主线或做通用化，现方案最大的阻碍是什么？

24. 你裁掉了哪些内核配置来缩小系统，裁剪时最容易误伤哪些功能？
追问：你有过“裁完能启动但某条链路悄悄失效”的案例吗？
追问：最难发现的误伤通常发生在驱动、文件系统还是调试工具支持？

25. remoteproc、rpmsg、mailbox、reserved-memory 在设备树里是怎么关联起来的？
追问：这几个节点之间的依赖关系你能按 probe 顺序讲一遍吗？
追问：如果 reserved-memory 配对了但 mailbox 中断没通，系统现象是什么？

26. 共享内存区域为什么通常要放到 reserved-memory，而不是随便找一块 DDR 用 /dev/mem 硬映射？
追问：如果不用 reserved-memory，最坏会和谁打架，CMA、buddy allocator 还是别的？
追问：你们有做过 cacheable/non-cacheable 属性区分吗？

27. 如果 remoteproc 能加载 R5F 固件但 rpmsg 通道起不来，你优先查哪几个内核点？
追问：是先看 dmesg 里的 virtio/rpmsg probe，还是先看 remoteproc state，为什么？
追问：如果 name service 没上来，你怎么判断是固件没创建 endpoint 还是 Linux 没绑定驱动？

28. 你怎么确认 cache coherency 问题是发生在 Linux A 核侧还是 R5F 侧？
追问：你会设计什么最小实验来定位，而不是靠猜？
追问：如果加上 cache flush/invalidate 后问题消失，你如何继续确认不是时序巧合？

五、Buildroot 与 RootFS

29. 你为什么选 Buildroot，而不是 Yocto？结合这个项目回答，不要泛泛而谈。
追问：这个选择在开发效率、包管理、可复用性上分别牺牲了什么？
追问：如果项目后面要做量产和多 SKU 维护，你还会坚持这个选择吗？

30. 你在 Buildroot 里定制了哪些 package、overlay、post-build 或 post-image 脚本？
追问：这些定制里哪些是为了让 remoteproc/rpmsg 一开机就可用？
追问：如果要别人一键复现构建，你最担心漏掉哪部分定制？

31. 你的 rootfs 里哪些东西是为了 OpenAMP/rpmsg 服务必须加进去的？
追问：这些是内核侧必须、用户态必须，还是两边都要？
追问：如果把这些东西去掉，最先缺失的现象是什么？

32. 如果系统启动后 /dev/rpmsg0 不存在，你会从 Buildroot、内核配置、设备树、用户态哪个层次开始查？为什么？
追问：请给一个你真实会执行的排查顺序。
追问：哪些层级的问题会导致“看起来像用户态问题，其实不是”？

33. 你如何把 R5F 固件、A 核应用、配置文件一起纳入构建产物，避免版本漂移？
追问：你们是否有统一版本号、git commit id 或 manifest 机制？
追问：如果现场机器跑的是错配版本，你最快怎么发现？

六、R5F 采集端与 OpenAMP/rpmsg

34. R5F 上 ADS8688 的采样触发机制是什么？是定时器驱动、SPI 中断驱动还是别的？
追问：这个触发机制最后决定了哪些抖动来源？
追问：采样时钟和数据搬运时钟是同一个节拍源吗？

35. Ping-Pong Buffer 为什么适合这个场景？相比 ring buffer，它解决了什么问题，又引入了什么限制？
追问：你的 Ping/Pong 切换是单 bit 状态、序号还是完整状态机？
追问：如果 A 核消费速度波动很大，Ping-Pong 相比 ring 的劣势会在哪里暴露？

36. 你通过 rpmsg 发送的到底是什么？是整块采样数据，还是“某块共享内存已满”的通知消息？
追问：消息体字段能具体列出来吗？
追问：如果只发通知，如何防止 A 核读到旧 buffer 或错 buffer？

37. 如果 rpmsg 只发通知，不发大块数据，那消息体里最少应该包含哪些字段？
追问：序号、长度、buffer id、时间戳里哪些是必须，哪些是为了调试加的？
追问：如果消息乱序或重复到达，你的协议如何自检？

38. 你如何避免 R5F 刚写完 buffer，A 核还没读，下一轮又覆盖掉？
追问：这是靠双缓冲就够了，还是还需要 ACK/credit 机制？
追问：如果 A 核持续慢于采集端，你是丢最新、丢最旧还是阻塞采集？

39. OpenAMP/rpmsg 这一层你踩过哪些坑？是 endpoint 创建、name service、virtio vring、还是 mailbox 中断？
追问：请说一个你亲自定位过的具体 bug，最后根因落在哪一层。
追问：这个 bug 最开始最误导你的表象是什么？

40. R5F 固件异常退出或卡死时，Linux A 核侧通常会看到什么现象？
追问：你们有没有做过心跳、超时或状态寄存器来区分“挂死”和“静默无数据”？
追问：如果 remoteproc 没显式报错，你靠什么证据判断是 R5F 死了？

七、共享内存设计

41. 你的共享内存布局是怎么设计的？头部、状态位、长度、序号、时间戳、payload 分别怎么放？
追问：头部是否做了对齐和 cache line 对齐，为什么？
追问：如果以后支持多 producer 或多 stream，这个布局还能不能复用？

42. 为什么你同时用了“共享内存 + rpmsg”，而不是只用其中一种？
追问：如果只用共享内存，没有 rpmsg，你要怎么做通知和状态同步？
追问：如果只用 rpmsg，没有共享内存，CPU 和中断开销会放大到什么程度？

43. /dev/mem 映射共享内存时，你怎么保证映射地址、长度、页对齐、缓存属性都正确？
追问：你在用户态是怎么验证自己映射到的就是预期物理地址的？
追问：如果映射成功但数据总不稳定，你先怀疑 mmap 参数还是 cache 属性？

44. 如果出现偶发脏数据、重复帧、半帧，第一反应你会怀疑共享内存的哪一层设计？
追问：这三种现象各自更像长度字段错、同步错还是 cache 错？
追问：你会先加序号、CRC 还是时间戳来帮助定位？

45. 共享内存上的生产者-消费者同步，你是靠中断、轮询、原子变量、内存屏障还是协议状态机？为什么？
追问：真正保证“先写数据后写状态”的是哪条机制？
追问：如果去掉内存屏障，只保留 volatile，最坏会发生什么？

46. 在 ARM 多核场景下，volatile 为什么不够？你真正依赖的同步手段是什么？
追问：请区分编译器重排、CPU 重排和 cache 可见性三个问题。
追问：你在 R5F 和 A 核两边分别用了什么 barrier 或 cache 操作？

八、A 核 Linux 多线程 Pipeline

47. 你的传输线程、DSP 线程、持久化线程、UI 线程之间的数据所有权怎么划分？谁能改，谁只能读？
追问：有没有哪份数据被多个线程共享可写？如果有，怎么控风险？
追问：你是传指针、切片视图还是做拷贝，为什么？

48. 你说用了 Lock-free Queue，具体是哪一种模型：SPSC、MPSC、SPMC 还是 MPMC？为什么选这个？
追问：这个选择和你的线程拓扑是一一对应的吗？
追问：如果后来新增一个持久化消费者，原模型还能不能直接沿用？

49. 如果 DSP 线程处理不过来，而采集线程还在持续进数据，你的背压策略是什么？
追问：这个策略会不会影响持久化线程和 UI 线程的一致性？
追问：你如何让系统“有损但可控”，而不是随机丢帧？

50. 你如何证明这个 Pipeline 在高负载下没有“隐性丢帧”？你看过哪些指标、日志或序号统计？
追问：你们有没有端到端序号，能把 R5F 采样块一路追到 WAV 文件？
追问：如果 UI 正常但 WAV 少数据，你怎么判断是哪一级丢的？

九、ALSA 与音频链路

51. ALSA 播放时你最终打开的是 hw、plughw 还是 default？为什么？
追问：如果换成另一个 PCM 名称，哪些行为会被 ALSA 悄悄帮你兜底？
追问：你们项目里为什么这个“兜底”可能反而是坏事？

52. 采集落盘是 WAV，播放时如何确保采样率、声道数、位宽与文件头一致？
追问：你是完全信任文件头，还是会和实际设备能力再做一次校验？
追问：如果文件头错了但数据本身没错，听感通常是什么样？

53. snd_pcm_hw_params 里哪些参数如果配错，最常见的表象是什么？
追问：请把 period、buffer size、format、rate、channels 分别对应的故障表象说出来。
追问：哪些参数 ALSA 可能帮你协商成功，但代价是引入隐式重采样或格式转换？

54. ALSA 的 period 和 buffer size 在你这个项目里怎么选？它们和实时性、CPU 占用、卡顿之间是什么关系？
追问：你实际选的数值是多少，为什么不是再小或再大一点？
追问：如果把 period 减半，你预期先恶化的是中断频率还是 underrun 风险？

55. 如果播放时出现 underrun，你会先改哪个参数或线程行为？
追问：你会先看应用写入节奏、磁盘读取还是 ALSA buffer 参数？
追问：如果只增大 buffer 能缓解，但延迟明显变差，你怎么权衡？

56. 你的播放端如果和采集端参数不一致，最容易出现什么听感问题？
追问：采样率不一致、位宽不一致、字节序不一致三者的听感差异分别是什么？
追问：有没有哪种错误在波形上能看出来，但人耳不一定第一时间听出来？

十、系统排障与真实性追问

57. 讲一个你亲自排过的“R5F 采到了数据，但 Linux 看起来像没数据”的问题，最后根因是什么？
追问：你是靠什么证据确认“R5F 确实采到了”，不是主观判断？
追问：如果让你重来一次，你会最先加哪两个观测点？

58. 再讲一个“rpmsg 正常，但共享内存数据错乱”的问题，最后是 cache、同步还是地址配置问题？
追问：为什么你一开始没有优先怀疑真正的根因？
追问：这个问题最后是怎么被彻底验证关闭的，不是“好像好了”？

59. 讲一个你在 U-Boot/Kernel 设备树里改了很久，最后发现是硬件接线或时钟源问题的案例。
追问：这个案例里最浪费时间的错误假设是什么？
追问：你后来建立了什么排查顺序，避免再踩同类坑？

60. 如果我让你现在现场检查这个项目的数据链路是否通，你会按什么顺序下断点、打 log、看节点、抓序号？
追问：请按 10 分钟内能执行完的最小排查路径回答。
追问：如果只能看一边日志，你选 R5F 还是 A 核，为什么？
