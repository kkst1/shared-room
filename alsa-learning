# ALSA 框架学习笔记（结合源码）

如果你把 ALSA 当成“一个驱动”，会很容易迷路。更准确的理解是：ALSA 是 Linux 音频子系统的一整套分层框架。它把“应用看到的统一接口”和“硬件差异极大的声卡实现”隔离开来。你真正要掌握的是三条线：对象模型、控制流、数据流。只要这三条线理顺，源码就不再碎片化。

先看全景。应用（`aplay/arecord/ffmpeg/播放器`）通常不直接碰内核，而是先走 `alsa-lib`。`alsa-lib` 再通过 `ioctl/read/write/mmap` 访问 `/dev/snd/*` 设备节点进入内核 ALSA Core。内核 Core 负责统一生命周期、状态机、并发和接口，再把具体动作下发给具体驱动。驱动可以是 PCI/USB/HDA，也可以是 ASoC（嵌入式最常见：CPU DAI + Codec + Machine）。这就是 ALSA 的主骨架。

---

## 1. ALSA Core：内核里的“总调度层”

核心目录在 Linux 内核 `sound/core/`。建议从这几份源码入手：

- `sound/core/init.c`：声卡对象创建与注册入口
- `sound/core/sound.c`：字符设备管理（`/dev/snd/*`）
- `sound/core/control.c`：控制接口（音量、开关、枚举等）
- `sound/core/pcm*.c`：PCM 全套逻辑（最重要）
- `sound/core/rawmidi.c`：RawMIDI
- `sound/core/timer.c`：ALSA Timer
- `sound/core/seq/`：Sequencer 子系统
- `sound/core/hwdep.c`：硬件相关透传接口
- `sound/core/compress_offload.c`：压缩音频 offload

ALSA 的顶层对象是 `snd_card`。你可以把它看成“一张声卡的内核实例”。驱动初始化时通常会做 `snd_card_new(...)`，创建 card 后再挂接 PCM、Control、MIDI、Timer 等子设备，最后 `snd_card_register(...)` 对用户空间可见。注册完成后，用户会在 `/dev/snd/` 看到节点，在 `/proc/asound/` 看到信息节点。

这一步非常关键：ALSA Core 不关心你底层是 I2S 还是 USB，它只要求你按统一对象模型注册。这个模型把复杂硬件抽象成统一可操作实体。

---

## 2. PCM：ALSA 最核心的实时数据通道

你学习 ALSA，80% 时间都应该花在 PCM。内核关键源码：

- `sound/core/pcm.c`：PCM 设备/子流管理
- `sound/core/pcm_native.c`：open/ioctl/read-write/mmap 主路径
- `sound/core/pcm_lib.c`：buffer、period、xrun、通用辅助逻辑

PCM 对象层次是：

`snd_pcm`（一个 PCM 设备）  
-> `snd_pcm_str`（playback/capture 两个方向）  
-> `snd_pcm_substream`（具体子流）  
-> `snd_pcm_runtime`（运行时参数和环形缓冲状态）

`snd_pcm_runtime` 是理解实时音频的核心。里面有你常见的采样率、格式、通道数、`buffer_size`、`period_size`，也有运行指针（硬件位置/应用位置）与状态机。用户空间不断写入环形缓冲，硬件 DMA 不断消耗，二者通过 period 中断或指针推进保持同步。

驱动侧必须实现 `snd_pcm_ops` 的关键回调，常见是 `open/close/hw_params/hw_free/prepare/trigger/pointer`。你可以把它们理解成“驱动合同”：

- `hw_params`：根据用户请求分配/配置 DMA buffer 与硬件参数
- `prepare`：在 start 前把硬件放到可运行状态
- `trigger`：真正开始/停止 DMA（`START/STOP/PAUSE/RESUME`）
- `pointer`：告诉 ALSA Core 当前硬件播放到哪

当 DMA 走完一个 period，驱动调用 `snd_pcm_period_elapsed(substream)` 通知 Core。Core 再唤醒阻塞写线程、推进状态。这个回调是“实时性”关键点，很多卡顿/爆音都和它的时机、粒度、锁竞争有关。

---

## 3. Control/Mixer：音频“控制面”的统一模型

控制面源码看 `sound/core/control.c`。它和 PCM 的数据面分离，负责音量、静音、路由、枚举模式等控制项。内核通过 `snd_kcontrol` 和 `snd_ctl_elem_*` 描述一个控制元素；用户态工具（如 `amixer`）通过 `SNDRV_CTL_IOCTL_*` 读写这些元素。

关键理解：Control 不搬音频数据，它只改“设备行为参数”。比如“耳机音量 80%”“输入源选 Mic”“旁路开关”。很多驱动里音量寄存器写入就挂在 kcontrol 的 `put` 回调中。

在 ASoC 中，kcontrol 还和 DAPM（动态音频电源管理）联动，改变路由时会触发部件上电/下电，这就是嵌入式设备省电的核心机制之一。

---

## 4. ASoC：嵌入式平台最重要的 ALSA 子框架

目录在 `sound/soc/`。如果你做板级音频（I2S + Codec），ASoC 是主战场。建议主读：

- `sound/soc/soc-core.c`
- `sound/soc/soc-pcm.c`
- `sound/soc/soc-component.c`
- `sound/soc/soc-dai.c`
- `sound/soc/generic/simple-card.c`（最常见入门 machine driver）

ASoC 的模型是三件套：`CPU DAI`、`Codec DAI`、`Machine/Card`。

- CPU DAI：SoC 侧 I2S/TDM/PCM 控制器接口能力
- Codec DAI：编解码芯片的数字音频接口能力
- Machine/Card：把板级连线关系、时钟关系、路由关系描述清楚（谁连谁、主从时钟谁给）

运行时，ASoC 会沿 `dai_link` 把一次 PCM 操作级联到 CPU/Codec/Platform 组件。比如 `hw_params` 会同时下发到 CPU DAI 和 Codec DAI，让双方在采样率、位宽、时钟上达成一致。任何一端不匹配，open 或 prepare 就会失败。

你做驱动时会频繁接触 `snd_soc_component_driver`、`snd_soc_dai_driver`、`snd_soc_card`。把它们理解成“功能实现”“接口能力”“系统装配”三层，会非常清晰。

---

## 5. 其他 ALSA 子系统：不是主线，但都属于完整框架

ALSA 不只有 PCM。一个完整认知还应包含这些：

- RawMIDI（`sound/core/rawmidi.c`）：原始 MIDI 数据通道，面向乐器/控制器场景
- Sequencer（`sound/core/seq/`）：MIDI 事件路由与调度网络，支持 client/port/event 模型
- Timer（`sound/core/timer.c`）：高精度定时机制，供音频/MIDI 调度使用
- HWDEP（`sound/core/hwdep.c`）：硬件相关透传接口（常用于 DSP/FW 交互）
- Compress Offload（`sound/core/compress_offload.c`）：压缩流硬件 offload（如低功耗播放）
- Proc/Info 接口（`/proc/asound/*`）：运行信息与调试观察入口

这几个你不一定每天写代码，但面试里常被问“ALSA 还包括哪些部分”。能把它们定位清楚，会明显体现体系化理解。

---

## 6. `alsa-lib`：用户态抽象层，决定了“应用看到的 ALSA”

用户态核心源码在 `alsa-lib` 项目里（不是内核树）：

- `src/pcm/pcm.c`、`src/pcm/pcm_hw.c`：PCM 基础与 hw 插件
- `src/pcm/pcm_plug.c`：格式/采样率自动转换插件（plug）
- `src/pcm/pcm_dmix.c`：软件混音（dmix）
- `src/control/control.c`：控制接口
- `src/mixer/`：Mixer 抽象
- `src/seq/`：Sequencer 用户态接口

应用最常见调用是 `snd_pcm_open`、`snd_pcm_hw_params_*`、`snd_pcm_writei/readi`。但 `alsa-lib` 可能会先把你连到插件链（`plug:`, `dmix:`, `dsnoop:`），不一定直接到 `hw:x,y`。这就是为什么同一应用在不同机器行为不同：配置文件和插件路由不同。

实际学习时建议你对比两种路径：

1) `aplay -D hw:0,0 xxx.wav`：几乎直达内核，问题最容易定位  
2) `aplay -D default xxx.wav`：经过插件栈，兼容性好但链路更复杂

理解插件栈对“采样率被偷偷转换”“多应用同时播音”的解释非常关键。

---

## 7. 一次完整播放调用链（从应用到硬件）

以播放为例，简化但源码对应的主路径如下：

应用调用 `snd_pcm_open` -> `alsa-lib` 解析设备名与配置 -> 建立 PCM 句柄  
-> `snd_pcm_hw_params` 系列设置 format/rate/channels/buffer/period  
-> 内核 `SNDRV_PCM_IOCTL_HW_PARAMS` 进入 `pcm_native.c`，转到驱动 `hw_params`  
-> `snd_pcm_prepare` 进入驱动 `prepare`  
-> `snd_pcm_start`/首次写触发 `trigger(START)`，DMA 开始搬运  
-> 驱动周期性 `snd_pcm_period_elapsed` 上报  
-> 用户继续 `writei` 填充 ring buffer，硬件持续消费  
-> 停止时 `trigger(STOP)`，释放由 `hw_free/close` 完成

ASoC 场景下，`soc-pcm.c` 会把这些动作分发到 CPU DAI/Codec DAI/Platform，实现端到端一致。

---

## 8. 学 ALSA 最容易卡住的点（以及你该怎么破）

最常见误区不是“不会写代码”，而是“状态机和层次混了”。比如把插件层问题当驱动问题，把控制面问题当数据面问题，把时钟/主从配置错误当成 DMA 问题。

一个高效学习路线是：

先只走 `hw:0,0`，聚焦内核 PCM 主链；再引入 ASoC 看 dai_link；最后再看 `default` 设备下的 `alsa-lib` 插件路由。每一步都抓三件事：当前对象是谁、当前状态是什么、下一跳是谁。你会明显感觉源码“连起来了”。

---

## 9. 你可以直接照着做的源码阅读顺序

先读内核 `sound/core/pcm_native.c`（用户请求怎么落到驱动），再读 `sound/core/pcm_lib.c`（buffer/period/xrun 机制），然后进 `sound/soc/soc-pcm.c`（ASoC 如何串联 CPU/Codec），最后回到 `alsa-lib/src/pcm/` 看用户态怎么封装与插件化。  
这条顺序是从“主链路”出发，能最快建立全局模型，不会陷入细节海。

如果你把上面这一套彻底吃透，ALSA 的核心架构就已经掌握了：  
它本质是“统一对象模型 + 分层调用链 + 严格状态机 + 可插拔实现”。
