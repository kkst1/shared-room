项目名称
基于 QEMU 的 RISC-V64 嵌入式操作系统（rCore C 语言重构版）

项目一句话描述（可直接放简历）
独立从零完成 RISC-V64 操作系统全栈研发，覆盖自定义 SoC 建模、Bootchain 打通、Sv39 虚拟内存、抢占式调度、系统调用与用户态加载，并实现双系统异构多核协同与可复现实验验证闭环。


一、项目目标与设计原则
1. 项目目标
- 构建可运行、可调试、可扩展的 RV64 OS 教学/实验平台。
- 打通“硬件模拟 -> 启动引导 -> 内核机制 -> 用户态应用”完整链路。
- 建立多核协同基础能力，为后续 IPC、驱动、文件系统扩展留接口。

2. 设计原则
- 最小可用内核优先：先跑通，再优化。
- 分层解耦：SoC 模型、引导层、内核子系统、用户态加载分层。
- 可观测优先：每个关键路径都可打点、可定位、可回归。


二、系统架构总览（模块分层）
1. 平台层（QEMU SoC）
- 自定义 RV64 SoC，挂接 UART、PLIC、CLINT、RTC、Flash、DRAM。
- 暴露 MMIO 地址空间与中断线，供 DTS 与内核驱动消费。

2. 引导层（Bootchain）
- Flash Bootloader：镜像装载与跳转控制。
- OpenSBI：M 态运行环境、SBI 服务、hart 管理与特权切换。
- Kernel Entry：进入 S 态后进行内核早期初始化。

3. 内核层（Kernel Core）
- 内存管理：物理页帧分配 + Sv39 页表管理。
- 中断/异常：trap 向量、时钟中断、系统调用入口。
- 进程与调度：PCB、上下文切换、抢占式时间片调度。
- 系统调用：`fork/exec/read/write` 等核心调用。

4. 用户层（User Space）
- `user_shell` 交互进程。
- 通过 `ecall` 触发系统调用。
- 基于 `incbin` 的静态应用打包与加载机制。


三、QEMU SoC 建模细节
1. 外设模型与地址映射（示例口径）
- UART：`0x1000_0000`
- CLINT：`0x0200_0000`
- PLIC：`0x0c00_0000`
- RTC：`0x0010_1000`
- Flash：`0x2000_0000`
- DRAM：`0x8000_0000`

2. 建模关键点
- 定义 `MemoryRegion` 与 MMIO 回调（read/write）。
- 中断通过 QEMU IRQ 路由到 PLIC/CLINT。
- 设备树节点（`compatible/reg/interrupts/clock-frequency`）与模型严格对齐。

3. 常见故障与修复
- 故障：内核探测不到设备。
- 根因：DTS `reg` 地址与 QEMU 映射不一致。
- 修复：统一地址定义头文件，启动时打印设备树解析结果校验。


四、Bootchain 全流程（数据与控制流）
1. 启动时序
1. Reset -> Bootloader 入口（Flash）
2. Bootloader 解析镜像头，加载 OpenSBI + Kernel 到 DRAM
3. 跳转 OpenSBI（M 态）
4. OpenSBI 初始化特权环境、定时器、hart 状态
5. OpenSBI `sbi_hart_start`/跳转到内核入口（S 态）
6. 内核早期初始化：BSS 清零、页表、trap、调度器、首进程
7. 拉起 `user_shell`

2. Bootloader 关键实现
- 镜像布局：`[header][opensbi][kernel][apps]`
- 基础校验：magic/version/size/checksum
- 失败回退：打印错误码并停机，避免野跳转

3. OpenSBI 集成要点
- 配置平台描述与设备树传递。
- 校验 SBI 调用链可用（console/timer/ipi）。
- 明确 M/S 态职责边界，避免重复初始化。


五、内存管理实现细节（MMU/Sv39）
1. 物理内存管理
- 页大小 4KB，维护空闲页帧栈（LIFO）。
- `alloc_page/free_page` O(1)。
- 记录高水位和分配失败统计用于压测分析。

2. 内核虚拟内存
- 早期恒等映射保证引导阶段可运行。
- 建立内核代码段只读可执行、数据段可读写映射。
- MMIO 区域映射为设备属性，禁止用户态访问。

3. 用户地址空间
- 每进程独立 `satp`，隔离代码段、数据段、堆、栈。
- `exec` 时重建地址空间并刷新 TLB（`sfence.vma`）。
- 页表项权限严格控制 `U/R/W/X`。

4. 缺页与异常处理策略
- 合法访问扩展（若实现按需分配）。
- 非法访问记录 `scause/stval/sepc` 并终止进程。
- trap 处理路径中禁止阻塞，避免二次异常放大。


六、调度器与中断系统
1. 时钟中断驱动抢占
- RTC/CLINT 周期中断触发 trap。
- trap 保存上下文后更新时间片计数。
- 触发 `schedule()` 选择就绪任务并完成上下文恢复。

2. 调度策略
- 默认 RR（Round Robin）保证公平与实现简洁。
- 任务状态：`NEW -> READY -> RUNNING -> BLOCKED -> ZOMBIE`
- runqueue 支持可扩展到优先级队列。

3. 上下文切换内容
- 通用寄存器、`sepc/sstatus`、内核栈、`satp`。
- 切换顺序：保存当前 -> 切地址空间 -> 恢复目标 -> `sret`。


七、系统调用子系统（Syscall）
1. 调用路径
- 用户态 `libc stub` 发起 `ecall`
- trap 进入内核 syscall handler
- 按 syscall number 分发到实现函数
- 返回值写回寄存器，返回用户态

2. 核心调用实现要点
- `fork`：
  - 复制父进程上下文与地址空间元数据
  - 子进程返回 0，父进程返回子 PID
- `exec`：
  - 卸载原地址空间
  - 装载新程序映像并设置入口/用户栈
- `read/write`：
  - 参数校验（用户地址合法性）
  - 分发到 UART/控制台或文件抽象层

3. 稳定性保护
- 用户指针检查与边界校验。
- 非法 syscall 号返回 `-ENOSYS`。
- syscall 慢路径打点，定位尾延迟问题。


八、用户态应用加载（incbin 静态注入）
1. 构建期流程
1. 编译各用户程序得到二进制
2. 脚本遍历 `apps/` 目录生成 `app_table.S`
3. `incbin` 注入每个 app 的字节流并导出起止符号
4. 链接进内核镜像

2. 运行期加载
- 根据 app 名称查询符号表拿到 `[start, end)`。
- 创建进程地址空间，按段拷贝到用户虚拟地址。
- 初始化用户栈与 `argv/envp`（按实现程度）。
- 设置 `sepc` 到用户入口并切换运行。

3. 方案优缺点
- 优点：无文件系统依赖、结构简单、启动快、教学友好。
- 缺点：应用更新需重编内核，不适合动态分发场景。


九、双系统异构多核协同（可面试展开）
1. 协同模型（示例）
- Hart0：主内核与调度控制。
- Hart1：独立服务核/实验子系统（如后台任务、监控或第二系统）。

2. 通信机制
- IPI 做事件触发。
- 共享内存队列传输消息（命令/状态/数据描述符）。
- 关键路径加入 memory barrier（`fence`）保证可见性顺序。

3. 一致性策略
- 单生产者单消费者队列降低锁开销。
- 共享结构 cacheline 对齐避免伪共享。
- 失败兜底：超时重试、心跳检测、故障隔离。


十、端到端数据流向（按运行时顺序）
1. 启动数据流
- Flash 镜像 -> Bootloader 解析 -> DRAM 装载 -> OpenSBI -> Kernel

2. 调度数据流
- RTC 中断 -> trap frame -> 调度器 -> 新任务上下文 -> 用户态

3. 系统调用数据流
- user_shell 命令 -> `ecall` -> syscall table -> 设备/内核服务 -> 返回结果

4. 应用加载数据流
- 内核符号表 -> app 字节流 -> 用户页映射 -> 入口跳转执行

5. 多核协同数据流
- Hart0 命令写入共享队列 -> IPI 通知 Hart1 -> Hart1 处理并回写状态


十一、调试与验证体系
1. 调试工具链
- QEMU `-d` 日志、GDB 远程调试、串口 trace。
- 关键 CSR 打印：`mstatus/sstatus/satp/scause/stval/sepc`。

2. 验证用例
- 启动链：每阶段成功标记与耗时统计。
- 内存：页分配回收一致性、越界访问保护。
- 调度：时间片轮转公平性、长期运行稳定性。
- syscall：`fork/exec/read/write` 正确性与异常输入测试。
- 多核：消息一致性与超时恢复。

3. 回归策略
- 脚本化启动并自动跑 smoke tests。
- 每次变更后输出“功能通过率 + 稳定性结果”。


十二、可替换量化指标模板（请替换为真实值）
- 冷启动到 shell ready：`[X] ms`
- 调度 tick 周期：`[X] ms`
- 单次上下文切换开销：`[X] us`
- syscall 平均延迟：`[X] us`
- 多核消息往返 RTT：`[X] us`
- 24h 稳定性测试崩溃次数：`0`


十三、简历可直接使用的强化版描述
独立完成基于 QEMU 的 RISC-V64 操作系统从 0 到 1 构建：自定义 RV64 SoC（UART/PLIC/CLINT/RTC）与设备树，打通 Flash Bootloader + OpenSBI + Kernel 启动链；实现基于 Sv39 的多级页表与进程级虚拟内存隔离，落地抢占式 RR 调度与 `fork/exec/read/write` 系统调用；设计 `incbin` 静态应用注入与自动符号表生成机制，支持 `user_shell` 启动与应用加载；完成双系统异构多核协同基础框架（IPI + 共享内存队列），并建立可回归的调试与验证体系。
