TI K3 异构 SoC 边缘音频数据管线项目压力面题库

说明：
以下问题站在大厂面试官视角设计，按“由浅入深、持续追问、逐层施压”的方式展开。
每个主问题后追加两个追问，重点覆盖硬件 Bring-up、U-Boot、Linux Kernel 移植与裁剪、Buildroot 根文件系统，以及你项目里的 remoteproc/rpmsg/OpenAMP、共享内存、音频 pipeline 等关键实现。

1. 你先完整讲一下这个项目的系统分层和启动链路，从上电到 R5F 开始采样、A 核开始处理音频，中间每一层分别负责什么？
追问 1：如果让你只保留 3 个最关键的软件层，你认为是哪 3 层，删掉任何一层会具体坏在哪里？
追问 2：你刚才讲的是理想路径，那第一次 bring-up 时最先卡住的点是什么，你怎么确认是卡在那一层而不是更下层？

2. 你说自己负责修改版 BeagleY-AI 板卡的 BSP 适配，那你拿到一块改版板之后，第一天会怎么制定 bring-up 顺序？为什么这么排？
追问 1：如果原理图和实际焊接版本存在偏差，你如何快速判断问题在硬件设计、焊接、还是软件配置？
追问 2：如果串口不出日志、网口不通、DDR 又不稳定，你会优先救哪一个观测口？为什么？

3. 你提到解决过“启动时串口无数据”的问题。现在你按面试现场复盘一次，不要只讲结论，要讲你的排障路径和每一步证据。
追问 1：为什么你会想到用 JTAG 看 PC 指针，而不是先怀疑串口 pinmux 或波特率？
追问 2：你说后来判断到 DDR 初始化问题，那你如何区分是 DDR training 失败、时序参数错误、还是代码跑飞后恰好把串口输出打乱了？

4. 如果现在板子上电后完全黑屏、串口静默，你作为 bring-up owner，最小可用的故障定位路径是什么？
追问 1：BootROM、SPL、U-Boot proper，这三个阶段你各自怎么确认“有没有跑到这里”？
追问 2：假设 JTAG 连得上，但 PC 卡在一个异常地址上，你怎么把这个地址映射回具体源码或链接脚本区域？

5. 你在这个项目里提到 PHY 驱动移植。讲一下你移植 PHY 驱动时具体改了什么，是 MAC 侧、MDIO 侧、PHY driver 侧，还是设备树侧？
追问 1：如果 MDIO 能扫到 PHY 地址，但 Linux 就是起不来链路，你第一反应看什么寄存器、什么状态位？
追问 2：如果 U-Boot 下网口能通，Linux 下不通，你如何判断是时钟、reset、pinmux、PHY driver 匹配还是 phylink/phylib 配置问题？

6. 修改版板卡通常会涉及 pinmux、电源域、时钟和 reset。你在设备树适配里最容易出错的是哪几类节点？为什么？
追问 1：一个外设“probe 成功但不能工作”，你怎么判断是设备树只配对了一半，还是驱动本身没真正拿到资源？
追问 2：如果同一个外设在 U-Boot 正常、Kernel 异常，你如何验证两边 dts/dtsi 差异到底影响了什么？

7. 你做了 U-Boot 移植和裁剪。请你具体讲一下在 TI K3 上，SPL、ATF、OP-TEE、U-Boot proper 各自处在什么位置，你裁掉了什么，为什么敢裁？
追问 1：如果裁剪后启动速度变快了，但某个外设在 Linux 阶段偶发初始化失败，你如何判断是不是早期 bootloader 阶段遗漏了关键初始化？
追问 2：你如何避免“为了让板子先起来，先在 U-Boot 里做了一堆 Linux 本该做的初始化”，最后导致责任边界混乱？

8. 你在 U-Boot 阶段具体做过哪些板级适配？请按“必须做”和“可选优化”分开讲，不要泛泛而谈。
追问 1：如果设备树、clock、DDR、console、bootargs 里只能优先确保两个点正确，你选哪两个？为什么？
追问 2：你有没有遇到过 U-Boot 能跑但是把后续内核启动环境污染掉的情况？如果有，怎么发现的？

9. 讲一下你做 Linux Kernel 移植时的策略：你是基于 vendor kernel、mainline 还是混合方案？为什么？
追问 1：如果面试官质疑你“只是改改 dts，不算内核移植”，你怎么证明你做的工作有内核层深度？
追问 2：如果让你把现有功能逐步向主线靠拢，你会优先处理哪些 patch，哪些 patch 暂时不碰？

10. 你说做了内核裁剪。请不要回答“为了减小体积”，我要听的是：你如何定义最小可用内核，以及你如何证明裁剪没有引入潜在运行时问题？
追问 1：哪些看起来“不用”的选项其实不能轻易关，比如 DMA、IOMMU、CMA、RPMsg、Remoteproc、debugfs，这些你是怎么权衡的？
追问 2：如果裁剪后镜像变小了，但音频链路在高负载下出现偶发问题，你怎么反推是不是内核配置被裁坏了？

11. Buildroot 根文件系统是你定制的。你先说说为什么选 Buildroot，而不是 Yocto、Debian rootfs 或手工 BusyBox 方案？
追问 1：如果面试官说 Buildroot 更像“比赛选手方案”，不够工程化，你怎么反驳？
追问 2：在你的项目约束下，Buildroot 带来的最大收益和最大代价分别是什么？

12. 你的 Buildroot 最小化 RootFS 里，哪些组件是为 remoteproc/rpmsg/OpenAMP 运行环境必须保留的，哪些是为了调试阶段刻意留下来的？
追问 1：如果要进一步缩 rootfs 体积 30%，你第一刀砍哪里，第二刀砍哪里？
追问 2：如果现场要求你解释一个包是“运行时依赖”还是“开发调试依赖”，你会按什么标准划分？

13. 你在 R5F 与 A 核之间做了“控制面和数据面分离”。为什么这不是一句架构口号，而是这个项目必须做的事情？
追问 1：如果你直接把大块音频数据走 rpmsg/vring，会在哪几个层面先出问题？请讲具体，不要只说“性能差”。
追问 2：如果面试官质疑“共享内存+通知”会让一致性更复杂，不如都走 rpmsg 简单，你怎么回应？

14. 你在 R5F 侧设计了共享内存 chunk buffer 和 rpmsg 事件通知机制。请讲清楚内存布局、ownership 切换、cache 维护和通知时序。
追问 1：如果某个 chunk 被 Linux 侧读到一半，R5F 又开始覆盖写了，你的设计靠什么避免这类撕裂问题？
追问 2：你所谓的 low-copy 到底是零拷贝、单拷贝还是有限拷贝？分别发生在哪些边界？

15. 你说 R5F 负责 ADS8688 高实时采样。请具体讲讲采样链路：触发方式、采样周期约束、中断/DMA 分工、缓冲策略，以及你如何保证实时性。
追问 1：如果采样没有丢，但时间抖动很大，最终 FFT 结果会受什么影响？你如何量化这个影响？
追问 2：如果采样速率继续翻倍，你认为先崩的是 ADC 接口、R5F 处理、共享内存链路还是 A 核消费？为什么？

16. 讲一下 A 核 Linux 侧的多阶段音频 Pipeline。为什么你用了 lock-free queue 和 frame view，而不是传统的“一个线程收、一个线程处理、一个线程存”串行模型？
追问 1：lock-free queue 在你的场景里真的必要吗？如果换成 mutex + condition variable，瓶颈会体现在哪？
追问 2：只读 frame view 怎么保证生命周期安全？谁拥有底层 buffer，谁负责回收，怎么避免悬空引用？

17. 你的 Pipeline 里有 FFT、UI 展示、持久化存储、WAV 回放。请你讲一下如何做背压控制，避免慢消费者把整个主链路拖死。
追问 1：如果 UI 线程卡 500ms，你的数据路径具体会怎样退化？是丢 UI 帧、丢音频帧、阻塞采集，还是触发降级策略？
追问 2：如果持久化线程写盘抖动很大，你怎么隔离 I/O 毛刺对实时处理路径的影响？

18. remoteproc/rpmsg 在你的项目里是怎么落地的？从 Linux 侧启动 R5F 固件，到 endpoint 建立，再到用户态拿到可用数据，完整链路你讲一遍。
追问 1：如果 remoteproc 拉起固件成功，但 rpmsg channel 没起来，你先查哪里？是 resource table、name service、endpoint、还是用户态绑定？
追问 2：如果现场让你解释 vring 和共享内存的关系，你怎么讲到既准确又不把自己绕进去？

19. 你做过数据链路优化。请你挑一个真实问题讲清楚：现象是什么、瓶颈在哪、你如何采样数据证明瓶颈存在、优化后指标怎么变。
追问 1：这些指标里哪些是平均值，哪些必须看 P99、最大值、连续稳定时长？为什么？
追问 2：如果某项优化把吞吐提高了，但系统可维护性明显下降，你怎么判断值不值得合进去？

20. 现在站在系统 owner 的视角，我假设线上出现“偶发音频缺块、频谱图偶尔毛刺、但日志基本正常”的问题，你会如何设计一套分层定位方案，区分问题在硬件采样、跨核传输、Linux Pipeline、还是存储/回放侧？
追问 1：你会埋哪些关键观测点？请给出至少一条从 R5F 到 A 核用户态的全链路时序证据。
追问 2：如果最后发现不是软件，而是板级电源完整性或时钟质量问题，你怎么向面试官证明你有能力把问题收敛到硬件层，而不是“软件查不出来就甩锅硬件”？
