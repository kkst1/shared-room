TI K3 异构 SoC 项目面试题（面试官视角）

说明：以下每题包含「考察点」「高分回答节点」「常见追问」，可用于技术面深挖。

1. 你们这套系统的整体架构为什么选择 A53 + R5F 分工，而不是全在 Linux 侧做？
- 考察点：架构设计能力、实时性理解、异构协同思维
- 高分回答节点：
  - R5F 负责硬实时采集（512 ksps，确定性调度，低抖动）
  - A53 负责复杂业务（UI、FFT/倍频程、存储、回放）
  - 通过控制流/数据流分离降低 rpmsg 压力，保证吞吐和时延
- 常见追问：
  - 为什么不用 PRU 或 DSP 核来采？
  - 采样时钟抖动对频谱影响怎么量化？

2. 你如何排查并修复 K3 多级启动链路问题（ROM→SYSFW→SPL→U-Boot→Linux）？
- 考察点：底层 bring-up 实战、系统化排障能力
- 高分回答节点：
  - 串口日志分段定位：先确认停在哪一级
  - 核查 boot media、SYSFW 镜像、SPL DDR 初始化、U-Boot 环境变量
  - 对照 dtb pinmux/clock/reset 配置，确认外设电源域和时钟域
- 常见追问：
  - SPL 阶段 DDR 初始化失败通常看哪些寄存器？
  - 如何缩短启动时间而不牺牲稳定性？

3. 你在 R5F 裸机下的 ADC 驱动怎么保证 512 ksps 零丢包？
- 考察点：驱动设计、吞吐预算、实时系统设计
- 高分回答节点：
  - SPI + DMA 双缓冲/环形缓冲，减少中断开销
  - 生产者消费者模型，采集中断只做搬运，不做复杂计算
  - 基于 watermark 和 overrun 计数器做丢包监控
- 常见追问：
  - 一帧大小怎么选？为什么不是越大越好？
  - 哪些路径会导致尾延迟（P99）变差？

4. OpenAMP 中你们如何实现“数据流与控制流分离”？
- 考察点：异核通信设计、协议设计能力
- 高分回答节点：
  - 控制流走 rpmsg（start/stop/mode/error）
  - 数据流走共享内存 ring buffer（无锁 + cache line 对齐）
  - rpmsg 仅传描述符（offset/len/seq/timestamp），不传大块数据
- 常见追问：
  - cache 一致性怎么处理？何时 flush/invalidate？
  - 序号丢失和乱序如何恢复？

5. 你怎么证明通信延迟控制在毫秒级？
- 考察点：可观测性和指标闭环
- 高分回答节点：
  - 端到端埋点：R5F 采样时间戳、A53 收包时间戳、渲染时间戳
  - 输出 P50/P95/P99 延迟，区分数据面与控制面
  - 压测不同 chunk size 和负载下的延迟曲线
- 常见追问：
  - 时钟源不一致如何对齐？
  - 如果出现周期性抖动，你会优先看哪里？

6. FFT 和倍频程你做了哪些工程化优化？
- 考察点：算法落地能力、性能优化能力
- 高分回答节点：
  - 预计算窗函数和 twiddle factor，减少重复计算
  - 分块处理 + SIMD/NEON（若启用）优化热点路径
  - 倍频程采用频带能量积分，带边界映射提前查表
- 常见追问：
  - 为什么选择该窗函数？频率分辨率与泄漏怎么权衡？
  - 你怎么校准幅值，保证结果可信？

7. UI 实时显示为什么会卡顿，你怎么定位并解决？
- 考察点：系统级性能优化
- 高分回答节点：
  - 采集线程、算法线程、渲染线程解耦
  - 环形缓冲 + 丢帧策略（保实时性优先）
  - 降低 UI 主线程工作量，固定刷新节奏（如 30/60 FPS）
- 常见追问：
  - 卡顿时是 CPU、内存带宽还是锁竞争瓶颈？
  - 你如何避免“越优化越不稳定”？

8. ALSA ASoC 的 PCM5122 驱动你做了哪些关键工作？
- 考察点：Linux 音频栈理解、驱动能力
- 高分回答节点：
  - Codec 驱动寄存器配置（I2C）+ DAI 参数协商（I2S）
  - machine driver 绑定 CPU DAI 与 Codec DAI
  - DTS 配置 clocks、sound card、routing、widgets
- 常见追问：
  - BCLK/LRCLK 与采样率关系怎么算？
  - 播放爆音通常怎么排查？

9. 你如何保证采集链路和离线回放链路互不干扰？
- 考察点：架构解耦、稳定性设计
- 高分回答节点：
  - 采集写入共享数据总线，回放从文件/缓存独立读取
  - 线程优先级区分，实时路径优先
  - 通过状态机限制模式切换时机，避免竞争
- 常见追问：
  - 切换模式是否会丢最后一帧？如何处理？

10. 这个项目里最难的技术问题是什么？你怎么闭环？
- 考察点：ownership、问题拆解能力
- 高分回答节点：
  - 明确问题定义（现象/触发条件/影响面）
  - 建立假设并逐项排除（日志、trace、profiling）
  - 最终方案 + 回归验证 + 文档化沉淀
- 常见追问：
  - 如果再做一次，你会提前做什么以避免踩坑？

11. 你们做了哪些可靠性与异常恢复机制？
- 考察点：工程成熟度
- 高分回答节点：
  - R5F 心跳 + A53 watchdog
  - ring buffer 溢出告警和自动降级（降帧/降带宽）
  - remoteproc 异常重启和状态恢复策略
- 常见追问：
  - 线上如何快速定位是 R5F 挂了还是 A53 堵了？

12. 你在这个项目中的个人贡献如何量化？
- 考察点：真实性与结果导向
- 高分回答节点：
  - 启动时间从 XX s 到 XX s
  - 通信吞吐达到 XX MB/s，端到端延迟 P99 < XX ms
  - 渲染稳定 XX FPS，CPU 峰值 < XX%
- 常见追问：
  - 指标是怎么测的？测试样本规模和工况是什么？

13. 如果让你继续迭代，你的路线图是什么？
- 考察点：技术规划能力
- 高分回答节点：
  - 阶段1：稳定性（自恢复、全链路监控）
  - 阶段2：性能（NEON/多核并行、零拷贝优化）
  - 阶段3：产品化（校准工具、报告导出、远程升级）
- 常见追问：
  - 哪一步投入产出比最高？

14. 你如何处理“高声压”场景下的前端饱和和失真问题？
- 考察点：信号链路认知
- 高分回答节点：
  - 增益分级和满量程设计（前端 + ADC）
  - 削顶检测、过载标记、动态范围评估
  - 校准曲线与设备一致性管理
- 常见追问：
  - THD+N 怎么测，结果怎样才算可接受？

15. 面试官可能重点质疑的点与应对
- 考察点：简历可信度
- 高分回答节点：
  - 所有 [X] 指标都准备测试方法、脚本、日志截图证据
  - 能说清“为什么这么设计”，而不是只说“做了什么”
  - 能给出失败案例和取舍逻辑（实时性 vs 准确性 vs 成本）
- 常见追问：
  - 哪项指标最难复现？为什么？


建议你面试前补齐 5 个关键“证据节点”
1. 启动阶段日志样例（每一级耗时）
2. OpenAMP 吞吐/时延压测表格（含 chunk size 对比）
3. FFT 精度验证报告（与 Matlab/Python 对照）
4. UI 性能剖析截图（线程/CPU/帧率）
5. ALSA 回放链路配置与波形验收记录
